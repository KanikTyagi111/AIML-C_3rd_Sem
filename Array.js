let c = [1,2,3,4,5];

// push
// c.push(6);
// console.log(c);


// pop
// c.pop();
// console.log(c);

// length of array
// console.log(c.length);

// unshift -> adds a value in front of the array
// c.unshift(0);
// console.log(c);

// shift -> removes the first value of the array
// c.shift();
// console.log(c);


// slice -> returns a portion of the array
// let d = c.slice(1,3);
// console.log(d);


// splice -> removes a portion of the array and adds new values(index, how many to remove, new values)
c.splice(1,2,7,8);
console.log(c);


// reverse -> reverses the array
// c.reverse();
// console.log(c);







